<!-- Navbar-->
<nav class="navbar navbar-default">
    <div class="container">
        <!-- Navbar brand + toggle-->
        <div class="navbar-header">
            <button class="navbar-toggle collapsed" type="button" 
                    data-toggle="collapse" data-target="#main-nav"
                    aria-expanded="false">
                    <span class="sr-only">Toggle Navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/campgrounds">YelpCamp</a>
        </div>

        <!-- Navbar content-->
        <div class="collapse navbar-collapse" id="main-nav">
            <form class="navbar-form navbar-right" 
              action="/campgrounds" 
              method="GET" >
              <div class="form-group">
                <input type="text" class="form-control" name="search" placeholder="Search Camps">
              </div>
              <input type="submit" class="btn btn-default"></input>
            </form>
            <ul class="nav navbar-nav navbar-right">
                <% if (!currentUser) { %>
                  <li role="presentation"><a href="/login">Login</a></li>
                  <li role="presentation"><a href="/register">Sign Up</a></li>
                <% } else { %>
                  <li role="presentation" class="dropdown">
                    <a class="dropdown-toggle" 
                      data-toggle="dropdown"
                      href="#"
                      role="button"
                      aria-haspopup="true"
                      aria-expanded="false">
                      Notifications
                    </a>
                    <ul class="dropdown-menu"> 
                      <li>
                        <a href="/notifs">See Previous Notifications</a>
                      </li> 
                    </ul>
                  </li><li role="presentation"><a href=""><%= currentUser.username%></a></li>
                  <li role="presentation"><a href="/logout">Logout</a></li>
                  
                <% } %>
            </ul>
        </div>
    </div>
</nav>